"# Technical Implementation Plan: Mobile UX, Public Checkout Configuration, and Discount Code Editing Fixes\n\nThis plan outlines the visual and layout refinements for mobile screens in the inventory list and bottom navigation bar, resolves the issue on the customer catalog checkout page where shipping and payment methods are not correctly selectable/forced based on project settings, and fixes the bug where discount codes cannot be edited due to property casing mismatches.\n\n## Proposed Changes\n\n---\n\n### [Component 1] Mobile Style and UX Refinements\n\nWe will optimize the mobile views for the inventory register variants list and the main bottom navigation tab bar.\n\n#### [MODIFY] [index.css](file:///c:/Users/perss/Desktop/Lager/frontend/src/index.css)\n- Implement premium mobile style overrides for `.variant-row`, `.variant-details-wrapper`, `.variant-attr`, `.variant-pricing`, `.variant-stock-actions`, and `.variant-stock-label` under the `@media (max-width: 576px)` media query.\n- Add horizontal overflow scrolling (`overflow-x: auto`) to `.nav-tabs-wrapper` on mobile, keeping items in their natural size (`flex: 0 0 auto`) and setting a comfortable minimum width so they scroll beautifully like a native application. Hide the scrollbar track for a clean, modern aesthetic.\n\n#### [MODIFY] [InventoryTab.tsx](file:///c:/Users/perss/Desktop/Lager/frontend/src/features/inventory/InventoryTab.tsx)\n- Replace the inline style on `.variant-row` with clean markup and CSS classes.\n- Wrap the attributes and price under `.variant-details-wrapper` to allow clean stacking/side-by-side positioning.\n- Add a descriptive mobile-only label `Antal i lager:` for the stock stepper so it sits beautifully on its own row on mobile devices.\n\n---\n\n### [Component 2] Customer Checkout Configuration Options Fix\n\nWe will fix the issue on the public page where a project configured for shipping-only (`delivery_method: 'shipping'`) or Swish-only (`checkout_mode: 'ecommerce'`) does not correctly activate those options or address for
<truncated 2854 bytes>